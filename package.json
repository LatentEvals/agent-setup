{
  "name": "agent-setup",
  "version": "0.1.0-alpha.0",
  "description": "One declarative source of truth for the skills, MCP servers, and instructions you want every AI coding agent to use.",
  "license": "MIT",
  "author": "Montana Flynn",
  "homepage": "https://github.com/latentevals/agent-setup",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/latentevals/agent-setup.git"
  },
  "bugs": {
    "url": "https://github.com/latentevals/agent-setup/issues"
  },
  "type": "module",
  "bin": {
    "agent-setup": "./dist/cli.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm run test && npm run typecheck"
  },
  "dependencies": {
    "@clack/prompts": "^1.3.0",
    "gray-matter": "^4.0.3",
    "smol-toml": "^1.6.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
